name: Boundary Guardrails

on:
  push:
  pull_request:

jobs:
  check-boundary-contract-docs:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Verify boundary + runtime contract docs
        run: ./.github/scripts/check_boundary_contract_docs.sh
